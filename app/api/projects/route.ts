import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({
    message:
      "Neon project persistence will be implemented in the next step.",
  });
}

export async function POST() {
  return NextResponse.json({
    message:
      "Neon project persistence will be implemented in the next step.",
  });
}
